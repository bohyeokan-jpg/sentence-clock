<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/widget_background"
    android:padding="10dp">

    <TextView
        android:id="@+id/widget_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="문장시계"
        android:textSize="10sp"
        android:textColor="#8A6438" />

    <!-- TextClock's "a" (AM/PM) symbol is locale-driven — see widget_medium/
         widget_large for the same note. Split into its own TextView (set in
         English by Kotlin) stacked above the h:mm TextClock, left-aligned,
         so AM/PM sits at the time's upper-left corner instead of inline. -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/widget_meridiem"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="AM"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#2B2620" />

        <TextClock
            android:id="@+id/widget_clock"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:format12Hour="h:mm"
            android:format24Hour="h:mm"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#2B2620" />

    </LinearLayout>

</LinearLayout>
